for creating this follow the steps.
step 1: first you have to install mongodb.
step 2: then go to the your installed mongodb directory using command line or window explorer.
step 3: now with that path open CMD and hit the command mongod. ( this will say "mongodb  waiting for connections on port 27017 ")
step 4: after that open new terminal/ CMD and hit the command mongo (this will run the mongodb with prompt now use your database like use <DBNAME>)
step 5: with all of that open postmen and use your end points  see the response.
